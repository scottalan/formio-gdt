{
  "name": "formio_drupal",
  "description": "Form.io in Drupal",
  "version": "0.1.0",
  "engines": {
    "node": ">=0.12.0",
    "npm": "2.x"
  },
  "private": true,
  "scripts": {
    "build": "rm -rf build/ && grunt",
    "si": "npm run build && grunt si"
  },
  "dependencies": {
    "grunt": "~0.4.5",
    "grunt-drupal-tasks": "~0.10.1",
    "zombie": "^2.5.1"
  },
  "devDependencies": {
    "grunt-shell": "^1.3.0"
  }
}
